import 'package:flutter/material.dart';

class NavbarActiveIcon extends StatelessWidget {
  const NavbarActiveIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
